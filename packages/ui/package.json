{
  "name": "@pk-literature/ui",
  "version": "0.0.0",
  "private": true,
  "description": "Shared shadcn/ui component set (repository-layout.md: \"used by web; future admin tools\"). Ships raw .tsx — no build step; consumers transpile it themselves (apps/web/next.config.ts's transpilePackages).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@radix-ui/react-slot": "^1.3.1",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.5.5"
  },
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "devDependencies": {
    "@pk-literature/eslint-config": "workspace:*",
    "@types/react": "19.0.1",
    "typescript": "^5.6.3"
  }
}
