# SPEC-13 --- Security

HTTPS only, WAF, IAM least privilege, Secrets Manager, KMS encryption,
JWT, HTTP-only cookies, CSRF protection, rate limiting, audit logging,
Razorpay webhook verification, editor/customer isolation. Acceptance: No
secrets in code, full auditability.
