# SPEC-14 --- Observability

CloudWatch Logs, Metrics, Alarms, X-Ray (future). Business metrics: feed
CTR, search latency, imports, checkout conversion. Operational metrics:
Lambda errors, RDS CPU, ECS health. Acceptance: dashboards and alerts
for all critical services.
