{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "format": "uuid"
    },
    "publisherId": {
      "type": "string",
      "format": "uuid"
    },
    "status": {
      "type": "string",
      "enum": ["completed", "failed", "partially_failed"]
    },
    "totalBooks": {
      "type": "integer"
    },
    "newBooks": {
      "type": "integer"
    },
    "updatedBooks": {
      "type": "integer"
    },
    "rejectedBooks": {
      "type": "integer"
    }
  },
  "required": [
    "runId",
    "publisherId",
    "status",
    "totalBooks",
    "newBooks",
    "updatedBooks",
    "rejectedBooks"
  ]
}