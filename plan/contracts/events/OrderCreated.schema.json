{
  "type": "object",
  "properties": {
    "orderId": {
      "type": "string",
      "format": "uuid"
    },
    "customerId": {
      "type": ["string", "null"],
      "description": "null for anonymous checkout (SPEC-06 Principles: Anonymous checkout supported)."
    },
    "total": {
      "type": "number"
    }
  },
  "required": ["orderId", "customerId", "total"]
}