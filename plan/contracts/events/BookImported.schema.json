{
  "type": "object",
  "properties": {
    "stagingBookId": {
      "type": "string",
      "format": "uuid"
    },
    "importRunId": {
      "type": "string",
      "format": "uuid"
    },
    "publisherId": {
      "type": "string",
      "format": "uuid"
    },
    "sourceRef": {
      "type": "string"
    }
  },
  "required": ["stagingBookId", "importRunId", "publisherId", "sourceRef"]
}
